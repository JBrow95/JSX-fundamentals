const myDiv = (
    <div>
        <h1>Jsx div element</h1>
    </div>
)

// Always make sure there is an outermost element

const noDiv = (
    <p>Oops</p>
    <a href="#">Wont work with not outer element</a>
)